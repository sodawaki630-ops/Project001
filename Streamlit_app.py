# app_notes.py
import streamlit as st

st.set_page_config(page_title="บันทึกข้อมูล", page_icon="📝", layout="centered")

st.title("📝 แอปบันทึกข้อมูล")

# สร้าง session state สำหรับเก็บโน้ต
if "notes" not in st.session_state:
    st.session_state.notes = []

# อินพุตสำหรับพิมพ์โน้ต
note = st.text_area("พิมพ์โน้ตของคุณที่นี่:")

# ปุ่มเพิ่มโน้ต
if st.button("➕ เพิ่มโน้ต"):
    if note.strip() != "":
        st.session_state.notes.append(note.strip())
        st.success("✅ เพิ่มโน้ตเรียบร้อย!")
    else:
        st.warning("⚠️ กรุณาพิมพ์โน้ตก่อนเพิ่ม")

# แสดงโน้ตทั้งหมด
st.subheader("📋 รายการโน้ตทั้งหมด:")
if st.session_state.notes:
    for i, n in enumerate(st.session_state.notes, start=1):
        st.write(f"{i}. {n}")
else:
    st.info("ยังไม่มีโน้ตในตอนนี้")

